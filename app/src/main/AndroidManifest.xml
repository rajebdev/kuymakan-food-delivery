<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rajebdev.kuymakan">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@mipmap/ic_launcher_kuy"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_kuy_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:fullBackupContent="true">
        <activity android:name=".auth.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".buyer.BuyerActivity">
        </activity>
        <activity android:name=".restaurant.RestaurantActivity">
        </activity>
    </application>

</manifest>